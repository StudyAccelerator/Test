# Callback requested sequence

Written 27 August 2026. Two emails on group "Callback requested" (196438369449805734), which the report's CallbackCard joins via requestCallback() in lib/mailerlite.ts: the instant confirmation (what the call covers, reply-with-your-question ask, uses {$diag_call_time}) and the day 2 phone-tag safety net (Zoom Scheduler link). Takers can be students or parents, so the copy is taker-neutral. Waleed still rings people himself; these emails wrap the promise in confirmation and a fallback. Cadence, build and switch-on: ../README.md.
