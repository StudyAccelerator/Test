# design-sync notes

- 28 Jul 2026: first sync of the hand-authored design-system/ folder into the existing "A-Level Accelerators Design System" project (re-adopted on Waleed's instruction). Replaced the old rough auto-generated system: deleted preview/ (16 cards), colors_and_type.css, the old README.md and SKILL.md, ui_kits/ (recreations of the pre-July-2026 homepage, old pricing and the retired workshop era), _ds_bundle.js and _adherence.oxlintrc.json. Kept assets/ (real logos, favicon, founder photos) and decks/ (sales decks, separate work) untouched.
- This repo's layout is off-script for the converter: no React build, no Storybook. Each preview is self-contained HTML with a `<!-- @dsCard ... -->` first line the Design pane indexes directly. No _ds_sync.json anchor is produced (honest omission); each re-sync diffs by hand against list_files.
- Standing rule (design-system/README.md): sessions that change the site's visual language update design-system/ in the same pass and note that a re-sync is due.
- 28 Jul 2026 (same day, follow-up): the old decks/ in the project went blank after the first sync because they linked ../colors_and_type.css (deleted with the old system). On Waleed's instruction the four old decks (study-series, study-accelerator, mentorship, subject-accelerators, plus index/deck-stage.js/deck-styles.css) were deleted and replaced with the three CURRENT sales decks from origin/claude/sales-decks content/decks/ (summer-accelerator, subject-accelerators, study-system; the old four-tier lineup has no separate mentorship deck any more). Each deck.html is fully self-contained (founder photo as data URI) and got a `<!-- @dsCard group="Decks" ... -->` line prepended before upload; render-checked in headless Chrome first. When a deck changes on the sales-decks branch, re-stage the same way (prepend the marker, upload, re-arm _ds_needs_recompile).
- Gotcha learned: within one plan, a delete pass that runs AFTER a write pass removes same-path files just written (decks/subject-accelerators.html was clobbered and had to be re-written). Delete old paths BEFORE writing new files that share a path.
- The app regenerates `_ds_bundle.js` and `_adherence.oxlintrc.json` on its own recompile; they reappeared after being deleted. App-owned, leave them alone in future syncs.
